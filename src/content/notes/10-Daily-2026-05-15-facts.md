---
_slug: 10-Daily-2026-05-15-facts
_vault_path: 10-Daily/2026-05-15-facts.md
tags:
- daily-facts
- context-distiller
source: multi
created: '2026-05-15'
title: 2026-05-15 凌晨 facts
updated: '2026-06-15'
type: daily
status: budding
---

# 2026-05-15 凌晨 facts

## Session 精華（00:05–04:01 CST）

- **System Map 時間戳 drift**：`generate_system_map.py --check` 因 `*Auto-generated: {timestamp}` 每次不同造成 false-positive drift detection。解法：hash 比對前過濾時間戳行。已在 S18（00:30 heartbeat）修復。
- **System Map skill 事實錯誤**：skill 舊版宣稱「Stores file hashes in maps/.map_hashes.json」，實際是 on-the-fly hash comparison，無持久化 hash 檔。v1.0→v1.1 修正。
- **Heartbeat 提案漂移**：autonomic 層 code 已實作 Phase 3 drift detection，但提案 STATUS 仍標「未實作」。Lesson: code 變更後要反向檢查提案文字，不能只看提案。已 patch 進 `heartbeat-v2-autonomous-maintenance` skill。
- **Memory 壓縮**：03:00 從 3133B（87.6%）壓至 2679B（74.9%），低於 75% 閾值。
- **Distiller pitfalls 更新**：`§` 分隔符導致 patch 重複匹配 + ENG slug→CN filename dedup 失敗，兩個坑都寫進 `context-distiller` skill。

## 自主筆記 sync

- `2026-05-15-Claws--Sandboxing` → vault（Karpathy Claws + Safe YOLO VM isolation）
- `2026-05-15-Agent-Architecture` → vault（agent loop / memory fallacy / prompt caching 三合一）
- `2026-05-15-google-scion-agent-orchestration` → **跳過**（同 GitHub repo 已有 May 14 筆記）

## Session 精華（04:02–08:02 CST）

- **Plan-drift sensor 上線**：`writing-plans` skill 的 STATUS block 現在有自動執法——heartbeat `plan_drift` sensor 每 30 分鐘掃描，超過 7 天的非完成計畫自動 flag。已寫入 `heartbeat-v2-autonomous-maintenance` skill + `references/plan-drift-sensor.md`。
- **CV-T2 priority**：cost 整合進 heartbeat report 被選為優先項目。heartbeat_state.json 已有 cost_24h 資料但不可見，整合後 Telegram 日報會顯示 token 用量和成本。
- **DeepSeek 定價 pipeline 診斷**：三層查詢鏈（hardcoded dict → OpenRouter API → `/v1/models`），DeepSeek 被歸為 `billing_mode="unknown"`，跳過動態查詢。hardcoded 字典有 `deepseek-chat`/`deepseek-reasoner` 但沒有 `deepseek-v4-pro`，導致 cost 顯示 $0。已更新 `hermes-agent > references/cost-visibility-tools.md`。
- **Gateway 重啟**：卡在 deactivating 狀態，`stop → sleep 2 → start` 標準流程修復。PID 125032，161.5MB。
- **Skill 更新**：三個 skill patch：`writing-plans`、`heartbeat-v2-autonomous-maintenance`、`hermes-agent`（cost-visibility-tools）

## 自主筆記 sync

- `2026-05-15-agent-tool-simplicity` → 已在 vault（heartbeat 預先同步）
- `2026-05-15-agent-cost-security-convergence` → 已在 vault（heartbeat 預先同步）
- `2026-05-15-agents-still-bad-at` → 已在 vault（URL dedup 確認）


## Session 精華（08:02–12:00 CST）

- **Gateway Shutdown Post-Mortem 系統上線**：`gateway_shutdown_analyzer.py` 以 TDD 建立（test + impl），heartbeat `_scan_shutdown_health` sensor 整合。`hermes-health-guardian` v1.0.0 → v1.1.0 新增 Gateway Shutdown Post-Mortem 章節（架構圖 + 文件清單 + hook placement 陷阱）。詳見 [[2026-05-15-hermes-gateway-shutdown-postmortem]]。
- **plan-review v1.3.0**：Completeness 維度新增 D4（函數簽名完整性 + 絕對路徑檢查）和 D5（implicit knowledge 檢測 — 依賴未明確宣告的假設）。詳見 [[2026-05-15-hermes-planning-d4d5-improvement]]。
- **writing-plans v2.2.0**：Self-Critique 強制第 4 欄「漏洞 4（D4/D5）」，確保計畫產出時已自我掃過這兩類盲點
- **heartbeat-v2-autonomous-maintenance v2.1.2**：cron session 會讓 `running_agents_count` = 1 但使用者不忙，需 cross-check `active_sessions` 平台類型。已寫入 Step 0 pitfall。
- **write_file 無 append 模式**：這是核心環境限制。要追加內容必須 `read_file` → 串接新內容 → `write_file`。已寫入 `plan-review` skill pitfall + MEMORY.md 永久記憶。
- **novel-reading**：`action=raw` 比 MediaWiki API (`action=query&prop=revisions`) 更簡單，直接回傳純文字，不需 JSON 解析。
- **exploration-note-system pitfall**：同日多篇筆記交叉覆蓋時，未追蹤 leads 可能已過期（後續筆記已涵蓋）。補入 `search_files` content search 確認步驟。
- **context-distiller H1 dedup fix**：Phase 2.5 grep 沒排除 `exploration-index.md` 導致假陽性，已 patch。
- **Session 斷片**：使用者抱怨 context continuity，agent 修復 TUI session state 顯示。

## 自主筆記 sync

- `2026-05-15-mysti-multi-agent-debate` → 已在 vault
- `2026-05-15-mcpc-mcp-cli-proxy` → 已在 vault

## Vault 狀態

- 3 orphan → 已修復（backlinks from daily facts）
- exploration-index +2 rows
- consolidated insight: [[2026-05-15-1200-hermes-consolidated-insight]]
