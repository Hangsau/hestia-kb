{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build-full": "python3 scripts/sync-vault.py && python3 scripts/build-graph.py && astro build && pagefind --site dist",
    "sync-vault": "python3 scripts/sync-vault.py",
    "build-graph": "python3 scripts/build-graph.py",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^5.18.2",
    "pagefind": "^1.5.2",
    "tailwindcss": "^4.3.1",
    "vis-network": "^10.1.0"
  }
}
